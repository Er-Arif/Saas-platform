[project]
name = "company-platform-gateway"
version = "0.1.0"
description = "API gateway starter for public service routing"
requires-python = ">=3.13"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.28.0",
  "pydantic-settings>=2.6.1",
  "redis>=5.2.0",
  "uvicorn[standard]>=0.32.0"
]

